// Linear search will work on a sorted
// array but is inefficient compared to binary
// search. It requires checking each element
// one by one, resulting in 𝑂 (𝑛) O(n) time
// complexity, whereas binary search is 𝑂(log⁡ 𝑛)
// O(logn).
